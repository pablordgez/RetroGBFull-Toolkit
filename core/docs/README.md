# RetroGBFull Engine Core Docs

This folder contains a Docusaurus site for the reusable engine core under `core/src`.

## Commands

```bash
npm install
npm start
```

The site runs on `http://localhost:3001`.